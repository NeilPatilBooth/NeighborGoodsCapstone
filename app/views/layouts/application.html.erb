<!DOCTYPE html>
<html>
  <head>
    <title>Rails Template</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <!-- Add Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  </head>

  <body>
    <nav style="padding: 10px; text-align: left;">
    <a href = "/" style="margin-right: 15px;  color: #007bff;"> List of All Furniture </a>

    <% if current_user !=nil %>
      <a href = "/users/sign_out" style="margin-right: 15px; color: #007bff;" > User Sign Out</a>
      <a href = "/users/edit" style="margin-right: 15px; color: #007bff;" > Edit Profile </a>
    <% else %>
      <a href = "/users/sign_up" style="margin-right: 15px; color: #007bff;" > User Sign Up</a>
      <a href = "/users/sign_in" style="margin-right: 15px; color: #007bff;" > User Sign In </a>
    <% end %>


    </nav>
  <hr>
    <div><%= notice %></div>
    <div><%= alert %></div>
    <%= yield %>
  </body>
</html>
