<!DOCTYPE html>
<html>
  <head>
    <title>Rails Template</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav>
    <a href = "/" > List of All Furniture </a>

    <% if current_user !=nil %>
      <a href = "/users/sign_out" > User Sign Out</a>
      <a href = "/users/edit" > Edit Profile </a>
    <% else %>
      <a href = "/users/sign_up" > User Sign Up</a>
      <a href = "/users/sign_in" > User Sign In </a>
    <% end %>


    </nav>
  <hr>
    <div><%= notice %></div>
    <div><%= alert %></div>
    <%= yield %>
  </body>
</html>
